#!/bin/bash

# --------------------
# Alias
# --------------------
#cmd
alias ll="ls -la --color=auto"
alias c="clear"
alias gitnuke="git fetch --all --prune && git branch -vv | grep ': gone]' | awk '{print $1}' | xargs -r git branch -d"
# configs
alias zshconfig="zed ~/.zshrc"
alias sshconfig="zed ~/.ssh/config"
alias config="zed ~/.config/ghostty/config"
# python
alias src="source $HOME/dev/.init.sh"
alias d="deactivate"
alias py="python3"
alias pip="pip3"
# elixir - phoenix
alias mps="mix phx.server"
alias mer="mix ecto.reset"
